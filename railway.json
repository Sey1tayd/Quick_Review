{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate || true && python manage.py createsuperuserauto || true && python manage.py collectstatic --noinput || true && gunicorn quizsite.wsgi --bind 0.0.0.0:${PORT:-8000}",
    "healthcheckPath": "/",
    "healthcheckTimeout": 150,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
