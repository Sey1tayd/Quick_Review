{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "bash -c '/opt/venv/bin/python manage.py migrate || true && /opt/venv/bin/python manage.py createsuperuserauto || true && /opt/venv/bin/python manage.py load_midterm_questions || true && /opt/venv/bin/python manage.py collectstatic --noinput || true && exec /opt/venv/bin/gunicorn quizsite.wsgi --bind 0.0.0.0:${PORT:-8000} --workers 2 --timeout 120 --access-logfile - --error-logfile -'",
    "healthcheckPath": "/health/",
    "healthcheckTimeout": 150,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
